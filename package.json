{
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.1.3",
    "prettier": "^2.5.1"
  },
  "scripts": {
    "format": "prettier --write \"**/*.{json,md}\"",
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.{json,md}": "prettier --write"
  }
}
